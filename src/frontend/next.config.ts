/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    // The default locale is Swedish
    defaultLocale: 'sv',
    // For now just Swedish, we'll add more languages later
    locales: ['sv'],
  },
}

module.exports = nextConfig
